#!/usr/bin/python3

def element_at(my_list, idx):
    """
    Get an element in a list at given index
    And return it
    """
    if idx >= len(my_list) or idx < 0:
        return

    return (my_list[idx])
